"use client";

/**
 * Placeholder: Live Notes will be re-implemented to work with the new
 * workspace file-tree model in a follow-up pass. For now it shows a
 * descriptive message.
 */
export function LiveNotesPanel() {
  return (
    <p className="text-sm text-muted-foreground">
      Live Notes will scan your workspace files for contextual matches.
      This feature is being updated for the new workspace structure.
    </p>
  );
}

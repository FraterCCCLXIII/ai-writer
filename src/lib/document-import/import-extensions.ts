"use client";

/**
 * TipTap extensions used for HTML ↔ JSON conversion (import/export).
 * Matches the manuscript editor schema (no slash-command extension).
 */
import { baseExtensions } from "@/components/editor/extensions";

export const importExportExtensions = baseExtensions;
